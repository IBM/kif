[tox]
envlist = mypy, py{39,310,311,312,313,314}
skip_missing_interpreters = true

[testenv]
commands = {posargs:py.test}
extras = all
passenv =
    DBPEDIA
    JAVA_HOME
    JENA_HOME
    PUBCHEM
    WIKIDATA

[testenv:mypy]
commands = mypy -p kif_lib
